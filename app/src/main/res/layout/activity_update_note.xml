<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_set_user_info"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical"
    tools:context="com.wzm.tasking.activity.UpdateNote">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_set_user_info"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?app_bg"
        app:navigationIcon="@drawable/ic_back"
        app:popupTheme="@style/AppTheme.PopupOverlay"
        app:title="@string/update_note"
        app:titleTextColor="@color/colorWhite"></android.support.v7.widget.Toolbar>

    <EditText
        android:id="@+id/et_set_note"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="20dp"
        android:hint="@string/input_note"
        android:inputType="text"
        android:textColor="@color/colorGray_3"
        android:textSize="15dp" />

    <Button
        android:id="@+id/bt_set_note"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bt_selector"
        android:text="@string/update_note"
        android:textColor="@color/colorGray_3"
        android:textSize="15sp" />

</LinearLayout>
